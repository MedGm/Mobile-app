import 'package:flutter/material.dart';

class TarlLoading extends StatelessWidget {
  const TarlLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
